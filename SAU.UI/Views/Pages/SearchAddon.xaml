<ui:UiPage  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" x:Class="SAU.UI.Views.Pages.SearchAddon"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SAU.UI.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="{DynamicResource MenuItemSearch}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:AutoSuggestBox Grid.Column="0"
                               Margin="0 8 8 0"
                               PlaceholderText="{Binding SearchBoxAllAddons}"/>

            <ui:Button Grid.Column="1"
                       Padding="6"
                       Margin="0 6 0 0"
                       HorizontalAlignment="Stretch"
                       Appearance="Primary"
                       FontSize="16"
                       ToolTip="{DynamicResource NewsContextMenuRefresh}"
                       Icon="ArrowClockwise48"/>
        </Grid>
    </Grid>
</ui:UiPage>
